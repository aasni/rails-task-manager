<h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
<a href="/tasks">back to your checklist</a>
<%= link_to "edit this task", edit_path() %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
